function calcularArea(base, altura) {
    let area = (base * altura) / 2;
    return area; // Envia o resultado de volta
}

// Como usar:
let resultado = calcularArea(10, 5);
console.log("A área é: " + resultado); // Saída: 25